name: ffi-ptrace
summary: Ruby FFI bindings for Linux ptrace.
description: Ruby FFI bindings for Linux ptrace.
email: postmodern.mod3@gmail.com
homepage: http://github.com/sophsec/ffi-ptrace
authors: Postmodern
has_rdoc: yard

requirements: libc

dependencies:
  ffi: ~> 1.0

development_dependencies:
  bundler: ~> 2.0
